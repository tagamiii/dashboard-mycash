import React from 'react';

const Transactions: React.FC = () => {
    return (
        <div className="p-8">
            <h1 className="text-3xl font-bold text-gray-900 mb-4">Transações</h1>
            <p className="text-gray-500">Histórico detalhado de movimentações.</p>
        </div>
    );
};

export default Transactions;
